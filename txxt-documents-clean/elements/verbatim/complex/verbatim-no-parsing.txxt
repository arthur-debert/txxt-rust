:: title :: Test Verbatim No Inline Parsing
:: description :: Verify that inline formatting markers are not parsed inside verbatim blocks

Verbatim with formatting:
  This has *asterisks* and _underscores_
  But they are `not` parsed as #formatting#
:: text

The above verbatim block should preserve all formatting markers literally.